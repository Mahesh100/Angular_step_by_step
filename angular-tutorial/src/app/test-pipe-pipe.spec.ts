import { TestPiprPipe } from './test-pipe-pipe';

describe('TestPiprPipe', () => {
  it('create an instance', () => {
    const pipe = new TestPiprPipe();
    expect(pipe).toBeTruthy();
  });
});
